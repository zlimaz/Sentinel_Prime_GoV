name: Automação do Bot de Notícias (DESATIVADO)

on:
  workflow_dispatch: # Apenas manual

jobs:
  run-news-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Desativado
        run: echo "Este workflow foi desativado. A lógica foi unificada no bot-schedule.yml."