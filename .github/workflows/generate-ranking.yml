name: Gerador de Ranking Quinzenal (DESATIVADO)

on:
  workflow_dispatch: # Apenas manual

jobs:
  generate-ranking:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Desativado
        run: echo "Este workflow foi desativado. A lógica foi unificada no sync-data.yml e bot-schedule.yml."